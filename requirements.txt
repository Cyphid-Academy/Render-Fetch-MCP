# Pinned so the deployment is reproducible. See AGENTS.md for the update policy.

# MCP transport. FastMCP owns the tool surface and the streamable-HTTP app;
# stateless_http lives on http_app()/run_http_async() as of 3.x (D2).
fastmcp==3.4.5

# Web server for the streamable-HTTP endpoint.
uvicorn==0.52.1
starlette==1.3.1

# Tiers 1 and 2.
httpx==0.28.1
trafilatura==2.2.0

# Content-encoding codecs. NOT optional: identity.py advertises Accept-Encoding
# derived from what is importable, so a missing codec silently costs realism,
# and advertising one we cannot decode would return undecoded bytes and push
# every page down to the expensive tiers.
brotli==1.2.0
zstandard==0.25.0

# Tier 2.5 -- browser TLS/JA3 impersonation.
curl_cffi==0.16.0

# Tier 3 -- anti-detection Playwright fork. Chromium itself is installed by the
# build command, never at runtime (D4).
patchright==1.61.2

# Selector scoping (D12).
lxml==6.1.1
cssselect==1.5.0

# capture_page: tile encoding and entropy scoring (section 11).
pillow==12.3.0

# Web Bot Auth signing in `declared` mode (section 3).
cryptography==50.0.0
